---
title: Wave Zoom
description: A wave player with mouse-wheel zoom, built on wavesurfer.js ZoomPlugin.
---

import { ComponentPreviewWrapper } from "@/components/component-preview-wrapper"
import { Steps, Step } from "@/components/steps"
import { Tabs, TabsList, TabsTrigger, TabsContent } from "@/components/tabs"

<ComponentPreviewWrapper name="wave-zoom-demo" />

## Installation

<Tabs defaultValue="cli">
<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

<Tabs defaultValue="pnpm">
  <TabsList>
    <TabsTrigger value="pnpm">pnpm</TabsTrigger>
    <TabsTrigger value="npm">npm</TabsTrigger>
    <TabsTrigger value="yarn">yarn</TabsTrigger>
    <TabsTrigger value="bun">bun</TabsTrigger>
  </TabsList>
  <TabsContent value="pnpm">
```bash
pnpm dlx shadcn@latest add @waves-cn/wave-zoom
```
  </TabsContent>
  <TabsContent value="npm">
```bash
npx shadcn@latest add @waves-cn/wave-zoom
```
  </TabsContent>
  <TabsContent value="yarn">
```bash
yarn dlx shadcn@latest add @waves-cn/wave-zoom
```
  </TabsContent>
  <TabsContent value="bun">
```bash
bunx --bun shadcn@latest add @waves-cn/wave-zoom
```
  </TabsContent>
</Tabs>

</TabsContent>
<TabsContent value="manual">

<Steps>

<Step>

Install the peer dependency.

```bash
npm install wavesurfer.js
```

</Step>

<Step>

Install the required shadcn primitives.

```bash
npx shadcn@latest add button slider skeleton
```

</Step>

<Step>

Copy `wave-cn.tsx` into your project.

<ComponentPreviewWrapper name="wave-cn" variant="codesource" />

</Step>

<Step>

Copy `wave-zoom.tsx` into your project.

<ComponentPreviewWrapper name="wave-zoom" variant="codesource" />

</Step>

<Step>

Update the import paths to match your project setup.

</Step>

</Steps>

</TabsContent>
</Tabs>

## Features

- **Mouse-wheel zoom** — scroll anywhere on the waveform to zoom in or out. Powered by wavesurfer.js `ZoomPlugin`, injected into `WavesurferPlayer` via `onReady`.
- **Live zoom indicator** — displays the current zoom level in `px/s` as you scroll.
- **Skip controls** — forward and backward buttons jump by `skipSeconds` (default `5s`).
- **Drag to seek** — click or drag anywhere on the waveform to jump to that position.
- **Custom waveform styling** — `waveColor`, `progressColor`, `barWidth`, `barGap`, and `barRadius` accept any CSS value including `var(--*)` tokens.

## Usage

```tsx
import { WaveZoom } from "@/components/waves-cn/wave-zoom"
```


## Examples

### Custom Colors

`waveColor` and `progressColor` accept any CSS value including `var(--*)` tokens.

<ComponentPreviewWrapper name="wave-zoom-custom-wave" />



### Custom Zoom Range

Control how fast and how far the user can zoom.

<ComponentPreviewWrapper name="wave-zoom-custom-range" />

## API Reference

| Prop | Type | Default | Description |
| ---- | ---- | ------- | ----------- |
| `src` | `string` | — | **(Required)** Audio URL. Changing it recreates the wavesurfer instance. |
| `url` | `string` | — | **Deprecated.** Alias of `src`, kept for v1 compatibility. |
| `title` | `string` | — | Track title displayed above the waveform. |
| `zoomScale` | `number` | `0.5` | Zoom magnification per mouse-wheel step. |
| `maxZoom` | `number` | `1000` | Maximum zoom level in px/s. |
| `defaultZoom` | `number` | `100` | Initial zoom level in px/s. |
| `skipSeconds` | `number` | `5` | Seconds skipped by the back / forward buttons. |
| `onPlay` | `() => void` | — | Fired when playback starts. |
| `onPause` | `() => void` | — | Fired when playback pauses. |
| `onFinish` | `() => void` | — | Fired when the track ends. |
| `onTimeUpdate` | `(currentTime: number, duration: number) => void` | — | Fired on every playback tick. |
| `waveColor` | `string` | `var(--muted-foreground)` | Waveform bar color. Accepts any CSS value including `var(--*)` tokens. |
| `progressColor` | `string` | `var(--primary)` | Progress overlay color. Same formats as `waveColor`. |
| `waveHeight` | `number` | `64` | Waveform canvas height in px. |
| `barWidth` | `number` | `3` | Bar width in px. |
| `barGap` | `number` | `2` | Gap between bars in px. |
| `barRadius` | `number` | `2` | Border radius of each bar. |
| `className` | `string` | — | Extra classes on the root element. |
| `style` | `CSSProperties` | — | Inline style on the root element. |
