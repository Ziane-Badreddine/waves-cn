---
title: Wave Speed
description: A wave player with variable playback speed control, built on wavesurfer.js.
---

import { ComponentPreviewWrapper } from "@/components/component-preview-wrapper"
import { Steps, Step } from "@/components/steps"
import { Tabs, TabsList, TabsTrigger, TabsContent } from "@/components/tabs"

<ComponentPreviewWrapper name="wave-speed-demo" />

## Installation

<Tabs defaultValue="cli">
<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

<Tabs defaultValue="pnpm">
  <TabsList>
    <TabsTrigger value="pnpm">pnpm</TabsTrigger>
    <TabsTrigger value="npm">npm</TabsTrigger>
    <TabsTrigger value="yarn">yarn</TabsTrigger>
    <TabsTrigger value="bun">bun</TabsTrigger>
  </TabsList>
  <TabsContent value="pnpm">
```bash
    pnpm dlx shadcn@latest add @waves-cn/wave-speed
```
  </TabsContent>
  <TabsContent value="npm">
```bash
    npx shadcn@latest add @waves-cn/wave-speed
```
  </TabsContent>
  <TabsContent value="yarn">
```bash
    yarn dlx shadcn@latest add @waves-cn/wave-speed
```
  </TabsContent>
  <TabsContent value="bun">
```bash
    bunx --bun shadcn@latest add @waves-cn/wave-speed
```
  </TabsContent>
</Tabs>

</TabsContent>
<TabsContent value="manual">

<Steps>

<Step>

Install the peer dependency.

```bash
npm install wavesurfer.js
```

</Step>

<Step>

Install the required shadcn primitives.

```bash
npx shadcn@latest add button slider switch label skeleton
```

</Step>

<Step>

Copy `wave-cn.tsx` into your project.

<ComponentPreviewWrapper name="wave-cn" variant="codesource" />

</Step>

<Step>

Copy `wave-speed.tsx` into your project.

<ComponentPreviewWrapper name="wave-speed" variant="codesource" />

</Step>

<Step>

Update the import paths to match your project setup.

</Step>

</Steps>

</TabsContent>
</Tabs>

## Features

- **Continuous speed slider** — smoothly adjust playback rate between `0.25x` and `4x` with fine-grained step control.
- **Preserve pitch** — toggle pitch preservation independently from speed so the wave stays natural at higher rates.
- **Live rate display** — a read-only badge shows the current playback rate as you drag the slider.
- **Drag to seek** — click or drag anywhere on the waveform to jump to that position.
- **No re-render on speed change** — speed is applied imperatively via `setPlaybackRate`, so the wave is never destroyed or recreated when changing speed.

## Usage

```tsx
import { WaveSpeed } from "@/components/waves-cn/wave-speed"
```

## Examples

### Custom Range

Narrow the speed range for a more focused control, e.g. podcast listening speeds.

<ComponentPreviewWrapper name="wave-speed-podcast" />


### Custom Colors

`waveColor` and `progressColor` accept any CSS value including `var(--*)` tokens.

<ComponentPreviewWrapper name="wave-speed-custom-wave" />


## API Reference

### WaveSpeed

| Prop | Type | Default | Description |
| ---- | ---- | ------- | ----------- |
| `url` | `string` | — | Wave file URL to load. |
| `waveColor` | `string` | `var(--muted-foreground)` | Wave bar color. Accepts CSS vars, hex, hsl, oklch. |
| `progressColor` | `string` | `var(--primary)` | Progress bar color. Same formats as `waveColor`. |
| `waveHeight` | `number` | `64` | Wave canvas height in px. |
| `barWidth` | `number` | `3` | Bar width in px. |
| `barGap` | `number` | `2` | Gap between bars in px. |
| `barRadius` | `number` | `2` | Bar border radius in px. |
| `minSpeed` | `number` | `0.25` | Minimum playback speed. |
| `maxSpeed` | `number` | `4` | Maximum playback speed. |
| `defaultSpeed` | `number` | `1` | Initial playback speed. |
| `step` | `number` | `0.25` | Slider step increment. |
| `className` | `string` | — | Root element class. |
| `style` | `CSSProperties` | — | Root element inline style. |